🐱‍💻 ¿Qué es Tomcat Server?
Apache Tomcat es un servidor de aplicaciones web especializado en Java.
👉 En palabras simples:
Tomcat es el programa que:
Recibe peticiones web (HTTP)
Ejecuta aplicaciones hechas en Java
Devuelve páginas web al navegador o a curl
📌 No es solo un servidor web como Apache o Nginx,
es un contenedor de aplicaciones Java.

🧩 ¿Qué tipo de aplicaciones corre Tomcat?
Tomcat ejecuta aplicaciones Java que usan:
Servlets
JSP (Java Server Pages)
Estas aplicaciones se empaquetan en archivos llamados WAR.

🌐 Ejemplo real
Cuando haces:
curl http://stapp03:6400

Tomcat:
Escucha en el puerto 6400
Recibe la solicitud HTTP
Ejecuta la aplicación Java
Devuelve la respuesta (HTML, JSON, etc.)

📦 ¿Qué es un archivo .war?
WAR significa:
Web Application Rchive
Es un archivo comprimido (como un .zip) que contiene:
Código Java compilado
Archivos HTML / JSP
Configuración de la aplicación
📁 Internamente tiene cosas como:
ROOT.war
├── index.jsp
├── META-INF/
└── WEB-INF/
    ├── web.xml
    └── classes/


🚀 ¿Qué hace Tomcat con un .war?
Cuando colocas un .war en:
/usr/share/tomcat/webapps/

Tomcat:
Detecta el archivo
Lo descomprime automáticamente
Ejecuta la aplicación
Sin que tú hagas nada más 👍

🌳 ¿Qué significa ROOT.war?
Este punto es muy importante.
🔹 En Tomcat, cada app tiene un “contexto”
Archivo WAR
URL resultante
app.war
http://host:puerto/app
test.war
http://host:puerto/test
ROOT.war
http://host:puerto/


⭐ ROOT.war = aplicación principal
Si el archivo se llama:
ROOT.war

Entonces la app se despliega en:
/

📌 Por eso funciona directamente con:
curl http://stapp03:6400

Sin /ROOT ni /app.

🧠 Analogía sencilla
Imagina Tomcat como un centro comercial:
Tomcat → el edificio
Puerto 6400 → la puerta
WAR → una tienda
ROOT.war → la tienda principal al entrar

🧪 ¿Qué pasaría si NO fuera ROOT.war?
Si el archivo fuera:
beta.war

La URL sería:
http://stapp03:6400/beta

Y no funcionaría la URL base.

✅ Resumen rápido
Tomcat
✔ Servidor de aplicaciones Java
✔ Ejecuta aplicaciones web
✔ Usa WAR para desplegar apps
ROOT.war
✔ Archivo de aplicación Java
✔ Se despliega en la raíz /
✔ Permite acceso directo a la URL base


📦 Paso 2: Instalar Tomcat
En la mayoría de estos entornos (CentOS / RHEL):
sudo yum install tomcat -y

📌 Esto instala:
Tomcat


Java (dependencia)


Archivos en /usr/share/tomcat



⚙️ Paso 3: Configurar Tomcat para usar el puerto 6400
Editar el archivo de configuración:
sudo vi /usr/share/tomcat/conf/server.xml

Busca esta línea (normalmente puerto 8080):
<Connector port="8080" protocol="org.apache.coyote.http11.Http11NioProtocol"
           connectionTimeout="20000"
           redirectPort="8443" />

✏️ Cámbiala a:
<Connector port="6400" protocol="org.apache.coyote.http11.Http11NioProtocol"
           connectionTimeout="20000"
           redirectPort="8443" />

Guarda y sal (ESC :wq).

🚀 Paso 4: Iniciar y habilitar Tomcat
sudo systemctl start tomcat
sudo systemctl enable tomcat

Verifica que esté corriendo:
sudo systemctl status tomcat

Debe aparecer active (running) ✅

📁 Paso 5: Copiar ROOT.war desde Jump Host
Desde Jump Host, copia el archivo al servidor:
scp /tmp/ROOT.war stapp03:/tmp


📦 Paso 6: Desplegar la aplicación en Tomcat
En App Server 3:
sudo cp /tmp/ROOT.war /usr/share/tomcat/webapps/

📌 Tomcat descomprime automáticamente ROOT.war
El nombre ROOT.war hace que la app se cargue directamente en /

🔄 Paso 7: Reiniciar Tomcat
sudo systemctl restart tomcat


🧪 Paso 8: Verificar que la aplicación funciona
Ejecuta:
curl http://stapp03:6400

✅ Si ves HTML o contenido de la app → TODO CORRECTO
